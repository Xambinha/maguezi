/* ============================================================
   MAGUEZI — As Chaves da Cidade (configuração Supabase)
   ------------------------------------------------------------
   Copia os dois valores do teu projecto Supabase:
   Project Settings (engrenagem) → API

   - Project URL       -> SUPABASE_URL
   - chave "anon public" -> SUPABASE_ANON

   NOTA DE SEGURANCA: a chave "anon" e feita para viver no
   navegador (e publica por desenho). NUNCA copies para o site
   a chave "service_role" — essa e a chave mestra.
   ============================================================ */

var SUPABASE_URL = "EDITA-O-URL";
var SUPABASE_ANON = "EDITA-A-CHAVE-ANON";